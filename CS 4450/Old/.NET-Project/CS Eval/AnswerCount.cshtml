@{
    Response.ContentType = "application/json";

    string json = null;
    if (Request["crn"] != null && Request["semester"] != null &&
        Request["year"] != null)
    {
        var crn = Convert.ToInt32(Request["crn"]);
        var semester = Convert.ToInt32(Request["semester"]);
        var year = Convert.ToInt32(Request["year"]);

        var answerCount = EvalSystem.EvalSystemDB.GetAnswerCount(crn, semester, year);
        json = EvalSystem.AnswerCountModel.ToJsonString(answerCount);
    }
     
}@Html.Raw(@json)
    
